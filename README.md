# Readme Docker Infra Production

1. Copy repo https://github.com/defremov-cscart/cscart-docker.git
```
git clone cscart-docker && cd cscart-docker && cp .env.example .env
```
2. Add values to .env and run
```
make run_prod
```
